const tickets = require("./models/tickets");
const ticketConfig = require("./models/ticketConfig");
const { CDTickets } = require("./classes/CDTickets");

module.exports = {
    tickets,
    ticketConfig,
    CDTickets
}